export const postCheckNameDuplicate = async (
	name: string
): Promise<boolean> => {
	// const res = await tokenInstance.post('/user/duplicate', { name })
	// return res.data

	// Todo: 서버 api 개발 시 console.log는 삭제
	console.log(name)
	return false
}
